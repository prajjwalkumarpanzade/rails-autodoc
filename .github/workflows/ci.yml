name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ruby: ["3.2", "3.3"]
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true
          bundler: "2.6.9"
      - name: Run RuboCop
        run: bundle exec rubocop
      - name: Run specs
        run: bundle exec rspec
      - name: Verify generated OpenAPI spec
        run: bundle exec rake autodoc:verify

  appraisal:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        appraisal: ["rails-7-0", "rails-7-1", "rails-8-0"]
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
          bundler: "2.6.9"
      - name: Install appraisal
        run: gem install appraisal --no-document
      - name: Generate appraisal gemfiles
        run: appraisal generate
      - name: Install appraisal gemfile
        run: appraisal ${{ matrix.appraisal }} bundle install
      - name: Run appraisal specs
        run: appraisal ${{ matrix.appraisal }} rake spec
